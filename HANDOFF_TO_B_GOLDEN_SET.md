# 面試黃金測試集：交接與覆核請求（A → B）

黃金集是**完整草稿**，缺的不是工作量，是**第二個標記者**。
`labeled_by` 目前只有我一個人，自己標完自己確認等於沒有覆核。

原本分工表上這是你 W1 D2–D3 的欄位。我做掉了大部分（選 JD、查技能字串本來就要動詞彙表），
但覆核這一步必須是你——這也是我們原本講好的「前 3 份兩人各標一次比一致率」。

---

## 現況

| 項目 | 狀態 |
|---|---|
| 10 格配對（真資料：persona_150 × jobs_all） | ✓ |
| 6 段真實 Android STT 逐字稿 | ✓ |
| 181 筆 `has`/`wants`/`said` 標記 | ✓ 草稿 |
| 8 組 `alias_decisions` | ✓ 草稿 |
| 詞彙表 110 → 286 條，`golden_pairs` **15/15 通過** | ✓ |
| 標記鍵覆蓋率 32% → **100%** | ✓ |
| **你的覆核** | ✗ ← 擋著 baseline |
| 29 筆未標記的新候選 | ✗ |

跑法：

```bat
python tools/interview_eval.py --cases fixtures/golden/interview ^
    --vocab fixtures/vocab/skills_v1.json --draft --by-case
```

---

## 第一組真實數字

```
集合          precision   recall   TP   FP   FN   不計分   未標記
履歷集             0.973    1.000   72    2    0     18      0
JD 集             0.975    1.000  116    3    0      3     24
提及集             0.952    0.488   20    1   21      3      5
漏講(端到端)        0.467    1.000    7    8    0     24      0
```

### 對你影響最大的一個數字

**漏講 precision 0.467** —— 我們報出去的漏講，**超過一半是假指控**。
使用者被告知「你沒有提到 Angular」，而她講了。

這不只是我的問題。**你的 why prompt 會替這些假指控寫出有說服力的理由**，
而且語氣跟真漏講一模一樣。使用者會照著一個他其實已經講過的點去改。

八筆假指控，兩種成因，修法完全不同：

| 假指控 | 成因 | 修法 |
|---|---|---|
| `Angular`／`Git`／`JavaScript` | STT 把英文詞轉爛，且**每次錯得不一樣** | 履歷條件式模糊匹配（W2） |
| `軟體程式設計`／`專案時間╱進度控管` | **展演式陳述**——她做了但沒說出名字 | 語意段（W2，目前沒開） |

### 三個好消息

履歷集與 JD 集 precision/recall 都在 0.97 以上——**管線結構是對的**，壞的只有提及那一段。

漏講 recall 1.000——真漏講一個都沒放過，問題純粹在多報。

排序 0.800——人工首選八成落在機器前三名，權重公式方向正確。

---

## 覆核要跑什麼（先講清楚，省你半天）

**只需要這一行，不需要 embedding、不需要 `jobs_all.jsonl`、不需要 Module repo：**

```bat
python tools/interview_eval.py --cases fixtures/golden/interview ^
    --vocab fixtures/vocab/skills_v1.json --draft --by-case
```

這條路是純字面與別名比對，秒跑完。語意段預設關閉，所以不會去載 bge-m3（2.3GB）。

`build_interview_golden.py` 需要 `jobs_all.jsonl`（沒進版控），但你不用跑它——
10 格已經生好了。

---

## 請你覆核的：53 筆裁決

全部在 `tools/label_interview_golden.py` 的 `ADJUDICATED`，每筆附理由。
**不同意就改那個 dict 重跑 `python -m tools.label_interview_golden`**，
不要直接改 JSON——理由要跟標記一起留著。

### 標記規則（先確認你同意這三條）

- **指名算 2** —— 原字出現：「我用 Angular 寫的」
- **展演算 2** —— 具體行為描述：「我排了每週進度表，每次開會確認進度」＝專案時間控管
- **只能推論算 0** —— 「我在餐廳打工過」推不出「抗壓性」

第二條是重心，T3 整段的意義都在它上面。

### 六筆最該看的

| 我的判定 | 理由 | 為什麼可能有爭議 |
|---|---|---|
| T1 `JavaScript` = **2** | 「用加巴screen」——加巴≈java、screen≈script | 走樣得很遠，你可能認為讀不出來 |
| T1 `Angular` = **1（不計分）** | 「這個框架」是懸空指涉，判不出她講了沒有 | 你可能認為證據夠強 → 2，或不夠 → 0 |
| T2 `Angular` = **2** | 「用了Andrew了這個框架」——有詞元佔著框架名的位置 | Andrew vs Angular 是不是太遠 |
| T3 `專案時間` = **2** | 展演：排訪談時間、設 deadline、追延遲 | 展演的門檻到哪裡 |
| T3 `Line` = **0** | 「設一個在LINE」其實是「設一個 deadline」 | 這是刻意留的假陽性測資 |
| T6 `PowerPoint` = **0** | 她**實際沒講**，不是轉寫錯誤 | 要點卡要求提到，但她跳過了 |

### 兩組合併裁決我判「不同技能」

這兩組會改變交集，是整條鏈的上游，錯了下游全錯：

- `專案管理` **≠** `專案管理架構及專案說明`
  （做專案的能力 vs 能講方法論的知識性要求）
- `帳務處理` **≠** `結帳作業與帳務處理`
  （日常記帳 vs 期末關帳的特定流程）

比照 `golden_pairs.v1` 對「財務報表製作≠報表彙整與管理」的保守判法。

---

## 還缺的：29 筆未標記

補完詞彙表後，JD 散文掃描新找到的技能，候選池裡沒有：

```
C++、CSS、Claude、ERP 系統、Node.js、React、Vue.js、
品牌行銷管理、報表製作、文書處理軟體操作、時間管理、溝通協調、溝通能力 ⋯
```

它們現在不計分，所以 **JD 集那個 0.975 是虛的**。多數 `has=0`（履歷沒有），
標起來快。加進 `case['labels']['skills']` 補 `has`/`wants`/`said` 三格即可。

---

## repo 與詞彙表：我做的兩個決定（告知，不是徵詢）

**一、Interview repo 只放面試模組的東西。**

我一開始把 `CareerSandboxModule` 整包帶進 `feat/a-gap`（`scorer.py`、`recommend.py`、
`retrieval/`、`schemas/domain.py` 等 20 幾個），原因是驗證詞彙表擴充要跑
`run_golden_pairs`，而它相依 `VocabNormalizer` 整條。已經拆掉了。

兩個 repo 維持分開。驗證回 Module repo 跑。理由是合併要動你已經 push 的東西
（PR #1），而你正在寫 A5 端點——為了少切一次目錄去打斷你，不划算。
`app/contracts/protocols.py` 也拆掉了：它跟 `interview_protocols.py` 是兩份不同的合約，
並存的話之後沒人知道該改哪一份。

**二、`skills_v1.json` 正本在 Module repo，Interview 這份是唯讀鏡射。**

留一份鏡射是因為你要覆核就得跑 `interview_eval`，跑它就要詞彙表——
要求你 checkout 兩個 repo 才能看一份黃金集，那個摩擦太大。

代價是兩份會分岔，用一條規矩壓住：**所有編輯只在 Module repo 進行，改完同步過來。**
Interview 這份不要直接改。

---

## 詞彙表變更：需要你知道

`fixtures/vocab/skills_v1.json` 從 110 條擴充到 **286 條**（jobs_all 前 200 名 ＋
黃金集缺的 23 條）。這是三邊共用的檔案，A1 檢索與 B1 計分都會受影響。

**已驗證：`run_golden_pairs.py` 15/15 格通過，逐技能 75/75。** 沒有退步。
退路留在 `fixtures/vocab/skills_v1.pre_expand.json`。

擴充過程中的兩個發現值得記：

**詞彙表覆蓋率不是提及偵測的瓶頸。** 補了 176 條，提及集 recall 完全沒動
（0.488 → 0.488，TP/FN 一個數字都沒變）。漏抓的是 `Angular`／`Git`，
它們在逐字稿裡是「Andrew」「gats」——詞彙表有沒有那條根本不相干。

**但補詞彙表會讓假指控變多。** 交集因為兩側都解析得更好而變大，
提及偵測卻沒跟上，於是 gap 變多、多出來的全是假指控（FP 6 → 8）。
**結論：詞彙表和提及偵測要一起走。在提及偵測修好之前，不要再往下補。**

---

## 對你自己交付項目的三個提醒

**一、`filler_count` 可以用，而且鑑別力很漂亮。**
實測每百字：T3 正常 3.0、T4 多講故事 6.4、T6 刻意講亂 9.8。單調、間距明顯、
完全吻合錄音指示。**這是目前整個模組唯一乾淨的量化訊號。**

但範圍變窄了：Android 會**全數移除**非詞彙填充音（「嗯」5→0、「呃」5→0），
詞彙型完整保留（「那個」2→2、「就是」7→8）。所以 `FILLERS` 已縮減為只含詞彙型。
**你的 prompt 不要宣稱它涵蓋全部口語習慣**，它涵蓋的是詞彙型填充詞。

**二、`avg_sentence_len` 不是句長。**
實測 69–86 字。那是她**停頓前講了多長**（STT 停頓 1–2 秒就自動送出，
段界＝送出點）。這其實更有用——它測語流連續性——但措辭不要講成「句子長度」。

**三、語意反轉是你比我更該擔心的事。**
「蠻有興趣」→「沒有興趣」，兩次獨立測試都出現。漏講判定受影響有限
（它看技能詞不看情態），但你的 `starParts`／`questionFeedbacks`／`subScores`
是直接讀語意的——一句反轉會讓回饋整段錯，而且錯得很有說服力。

值得跟怡君討論一件事：**讓使用者看得到自己的逐字稿**。他一眼就能發現
「我明明說蠻有興趣」。這比後端做任何補救都有效，成本只是多一個畫面。

---

## 覆核完之後

```bat
:: 三個旗標翻 true 後
python tools/interview_eval.py --cases fixtures/golden/interview ^
    --vocab fixtures/vocab/skills_v1.json ^
    --write-baseline data/interview_baseline.json
```

工具會擋住三種情況：未覆核的草稿、覆蓋率低於 80%、含合成逐字稿。
所以只要它寫得出來，那份 baseline 就是可信的。

那是之後每次回歸的比較基準，也是 W1 的交付證據。

---

## 三個必須寫進報告的限制

**單一講者。** 六段都是同一個人錄的，英文技術詞的失敗率是「這個講者在這支手機上」。

**有效樣本數是 6 不是 10。** 同一段逐字稿服務多格，誤差相關。

**轉寫不穩定。** 同一個 `Angular` 出現三種寫法（`Android` / 整個消失 / `Andrew`），
`Git` 三種（`gate`/`gats`/`Gate`）。所以靜態別名表對英文技術詞是走不通的——
問題不是「有一個固定的錯誤形式」，是「每次都錯得不一樣」。
