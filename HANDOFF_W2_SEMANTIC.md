# W2 A 側交接：語意段校準（A → B）

分支：`CareerSandboxInterview` → `feat/a-semantic`

W2 D3–D5 的「A5 門檻調參」做完了。三個參數從「憑常識填」變成有實測依據，
**假指控從 9 筆降到 3 筆**，四個門檻全數通過。

---

## 三件最重要的（其餘可以之後再看）

**一、正式環境的漏講功能還不能用。** 你的抽取器對同一批 JD 的召回率是 0.200
（型錄是 1.000），真漏講從 8 筆掉到 3 筆。語意段方向對但量級不夠。
問題不只是抓得少，是**產出的詞跟詞彙表不同源**。建議改成從詞彙表選詞，
不要自由生成。→ 第十節

**二、剩下 3 筆假指控的成因全是展演式陳述，沒有一筆是 STT 轉爛。**
工具型技能（Excel、Git、Python）的漏講現在幾乎都是真的；
抽象能力型（專案管理、軟體程式設計）還有殘留。
你的 why 否決若要設優先序，抽象能力型該排前面。→ 第三節

**三、`TranscriptAnalyzer` 有 singleton 併發問題，我修了，API 變了。**
`near_misses()` / `residuals()` 已移除，改用 `analyse(t)`。
你 W3 接 lifespan 一定要知道。→ 第九節

**那 224 筆未標記篩完了，實際要標的只有 3 筆。** → 第十一節

---

# 一、先跑這個看數字

```bat
git fetch && git checkout feat/a-semantic
python tools\interview_eval.py --cases fixtures/golden/interview ^
    --vocab fixtures/vocab/skills_v1.json ^
    --stt-aliases data/stt_confusions.v1.json ^
    --semantic --by-case
```

**會載 bge-m3（約 2.3GB），跑一分鐘左右。**

只想秒跑的話拿掉 `--semantic`——但那量的是表面掃描，不是完整管線，
數字會是舊的 0.471。報表最上方會標明模式，不會搞混。

---

# 二、W2 的結果

| | W1 結束 | W2 結束 |
|---|---|---|
| 履歷集 | 1.000 / 1.000 | 1.000 / 1.000 |
| JD 集 | 0.965 / 1.000 | 0.965 / 1.000 |
| 提及集 | 0.952 / **0.488** | 0.729 / **0.854** |
| 漏講 | **0.471** / 1.000 | **0.727** / 1.000 |
| 排序（人工首選落在前三名） | 0.600 | **1.000** |

**假指控 9 → 3，而真漏講一筆都沒漏**（漏講 recall 全程 1.000）。

## 校準後的三個參數

```
SEMANTIC_WINDOW      28 → 16      語意段的滑動視窗
MENTION_THRESHOLD  0.55 → 0.45    一般門檻
CANDIDATE_THRESHOLD     → 0.40    履歷候選的放寬門檻（新增）
```

**視窗比門檻更關鍵**，這點我原本沒想到。實測 recall：視窗 12 → 0.805、
16 → 0.805、20 → 0.707、28 → 0.561。原因是長視窗把一整段話拿去跟四個字的
技能名比相似度，中間的「我那時候」「然後」全是雜訊，餘弦分數被稀釋。

門檻取 0.45 而非掃描器推薦的 0.40：視窗 16 在 0.40／0.45／0.50 三個門檻的
四項指標完全相同，是一片**平原**；0.40 在掃描範圍邊緣，不知道再低會不會崩。
**參數落在平原中間比落在邊緣穩健。**

---

# 三、對你 prompt 最重要的一件事

## 剩下 3 筆假指控的成因**全部是展演式陳述**

```
ivw-001／002   軟體程式設計
ivw-006        專案管理
```

**沒有一筆是 STT 轉爛。** `Git`／`JavaScript`／`Angular` 全被語意段救回來了——
這比我預期的好，我原本以為「gats」跟 Git 在語意空間沒有關係。

剩下這兩個是「做了但沒說出名字」。Victor 講的是
「我負責約訪談時間、有人拖延就傳訊息設 deadline、追他在期限前完成」，
但那跟「專案管理」這四個字的語意相似度**降到 0.20 都構不到**——
不是「差一點」，是根本不在附近。

原因：**抽象標籤跟具體行為在 bge-m3 的空間裡屬於不同的語意層級。**
這不是門檻問題，也不是 bug，是目前方法的限制。

### 所以你的 why prompt 要注意

漏講清單裡若出現**抽象能力型**的技能（專案管理、軟體程式設計、溝通協調
這一類），它是假指控的機率明顯高於工具型技能（Excel、Git、Python）。

工具型的漏講現在幾乎都是真的——語意段＋STT 別名把偽陽性清掉了。
抽象能力型的還有殘留。

不是要你分兩套語氣（我 W1 提過、你當時說過濾優於委婉語氣，那個判斷我同意），
而是**如果你的 why 否決機制要設優先序，抽象能力型該排在前面被檢視**。

---

# 四、兩個代價，講清楚

## 提及集 precision 0.952 → 0.729

誤抓從 1 筆變 13 筆：`HTML/CSS`、`會計或審計`、`簡報表達`、`文中系統` 等
被算成講到了。

方向是安全的（少給一條建議 vs 假指控），而且**漏講 recall 維持 1.000**
證明沒有吃掉真漏講。但這個保證建立在 6 段逐字稿上，樣本一大未必成立。

## 未標記從 32 筆暴增到 224 筆 ← 值得你看一眼

語意段會對整份 222 條詞彙表做最近鄰，掃出一堆候選池外的東西：
`Gemini`、`A/B 測試`、`主動聆聽`、`Claude`⋯⋯

它們不計分所以不影響指標，但**它說明語意段在放寬的同時也在大量誤命中**，
只是那些誤命中剛好落在候選池外。換一批 JD 或一份不同的履歷就未必。

這也表示 JD 集那個 0.965 仍然是虛的——那 224 筆沒人標過。
（W1 交接時是 29 筆，你還沒標，現在變 224 筆了。標記量變大了，抱歉。）

---

# 五、沿路抓到兩個「沒報錯」的漏

兩個都是同一類：**東西是對的，但接的方式錯了，而測試全綠。**

## 1. `stt_confusions.v1.json` 做好兩週，沒有任何程式讀過它

W1 探測完就產出的 STT 轉寫對照表，躺在 `data/` 裡看起來像交付物，
實際上是一份文件。`TranscriptAnalyzer` 有 `stt_aliases` 參數，只是沒人傳。

接上之後提及 recall **0.829 → 0.854**（剛好越過門檻），
`C#` 從漏抓清單消失——那是 `C Sharp`，C# 的口語形，不是辨識錯誤。

`interview_eval` 現在有 `--stt-aliases` 旗標。**碰撞別名（`Android`→`Angular`）
不載**，那要靠履歷條件判定，載進去會讓真正的 Android 永遠抓不到。

## 2. 接上它的時候打亂了索引優先序

`SurfaceIndex.from_vocab` 本來是兩趟：先登記**全部**完整表面形，再登記詞頭，
`setdefault` 保證完整形優先。我把別名合併寫在外面、逐條目做「完整形→詞頭」，
於是**第一條的詞頭贏過第二條的完整形**。

「報表彙整與管理」的詞頭是「報表彙整」，剛好撞上「報表彙整」這個獨立條目——
同一句話解析到不同技能，不當掉、不報錯，只有履歷集 recall 從 1.000 掉到 0.958。

如果我沒盯著那一欄，這會被當成「載入別名的正常代價」放過去。

已改成 `from_vocab` 直接吃 `extra_aliases`，三趟全域分開：
**完整形 → STT 別名 → 詞頭**。加了測試守著。

---

# 六、工具與 baseline 的變更

## `interview_eval` 新增三個東西

| 旗標 | 用途 |
|---|---|
| `--semantic` | 開語意段。預設關閉（你覆核黃金集時秒跑完） |
| `--stt-aliases` | 載 STT 轉寫對照表 |
| `--by-case` | 逐格分解。**「哪一格壞掉」比「整體平均」有用得多** |
| `--jd-source` | `catalog`（型錄）或 `extracted`（你的抽取器）。見第十節 |

## baseline 分兩份，而且不能互比

```
data/interview_baseline_surface_catalog.json    表面掃描（漏講 P 0.471）
data/interview_baseline_semantic_catalog.json   語意段（漏講 P 0.727）
```

檔名自動帶模式與 JD 來源，檔案內部也記模式、JD 來源與三個參數。
**模式或 JD 來源不符的 baseline 會被擋下不比對**——兩者量的是不同管線，混在一起會讓回歸失去意義：
看到「退步」時分不出是程式壞了還是模式不同。

參數跟數字一起凍也是為此——回歸時看到差異，要能分辨是程式改了還是參數改了。

## 四個防呆

模式不符的 baseline 會擋下；模型載不起來**直接炸而不退回表面掃描**；
假向量拒絕寫 baseline；未覆核的草稿標記拒絕寫 baseline。

第二個最重要：退回之後數字看起來完全合理（0.471 是個正常數字），
但它會被貼上 `semantic` 的標籤凍進 baseline，之後每次回歸都在跟一個
標錯模式的基準線比，而且沒有任何跡象。

---

# 七、營運提醒

**語意段要載 bge-m3（約 2.3GB）。** 合約 docstring 已註明
`TranscriptAnalyzer` 必須是 app 啟動時載入的 singleton，
不能每個 request new 一個。你端點組裝時的 FastAPI lifespan 要涵蓋它。

**詞彙表向量有全域快取**（`transcript._VOCAB_VEC_CACHE`）。
沒有它的話調參會跑到天亮——實測 4 視窗 × 7 門檻 × 10 格 = 14.6 萬次向量化、
2.7 小時。加了快取加上 `_semantic_pass` 不再內建門檻，整輪掃描只算一次向量。

正式環境詞彙表不變，所以那份快取一次算完就固定。**但詞彙表若在 runtime
被換掉，快取的 key 是 `id(embedding)` 加表面形 tuple，會自動失效，不用手動清。**

## 前端逐字稿分段：三個欄位有兩個還沒進 `TurnDTO`

怡君已經修好 STT 截斷問題（停頓不再被判定講完，改為累加＋自動接續聆聽），
並在**主 repo** 產出三個欄位。但後端這側的 `TurnDTO` 只有一個：

| 前端 | `TurnDTO` |
|---|---|
| `answer_segments` | ✅ 名字一致，不用改 |
| `answer_text` | 你這邊叫 `answer`（既有欄位），她會改用你的 |
| `segment_starts_ms` | ❌ **還沒有，要加** |

`segment_starts_ms` 的筆數**可能比 `answer_segments` 多 1**——多的那筆代表
「使用者按下結束前系統正要開始聽新的一段」。我跟她建議加一格
`ended_by: "user" | "timeout"` 把意義寫出來，因為現在資訊藏在長度差裡：
任何人 `zip()` 起來會安靜砍掉多的那筆，而三個月後有人看到長度不一樣
可能以為是 bug 去「修正」它。哪種做法你決定。

**一個對黃金集的影響**：那 6 段逐字稿是怡君**手動分段**貼給我的（舊行為，
停頓就自動送出），所以我的 `segmentation="stt_segment"` 標的是
「STT 自動送出點」。修好之後語意變成「引擎重啟點」——方向一樣
（都是語流連續性），但等真的接上端點，我要拿新格式重驗一次 `text_stats`。

---

# 八、還沒做的

## ~~`CollabObserver`~~ ← 已交付

見第十節。

## 那 3 筆假指控

要解決得把技能的**行為描述**放進詞彙表當別名
（「排時程、追進度、設期限」→ 專案管理），拿行為對行為比。

那是詞彙表工程不是參數調整，工作量不小。**W3 值得評估，但不該倉促做**——
現在 0.727 已經是可用的數字。

## ~~那 224 筆未標記~~ ← 已分流，見第十一節

實際要標的只有 3 筆。

---

# 九、W2 收尾的兩項交付

## 1. `CollabObserver` 已交付（`app/pipeline/collab.py`）

四項裡兩項有實質輸出：

```
參與主動性   first_speak_position / framing_in_first / initiated_after_gap
論點建構     causal_connector_rate / avg_utterance_chars / utterance_length_sd
傾聽與回應   signals={}，evidence 註明「無他人論點可比對——此項不可用」
協作姿態     signals={}，同上
```

`level` 一律 `None`，走你既有的 notices 路徑。**可以把 `FakeCollabObserver`
換掉了**——你 W3 D3–D4 要做 group persona 和端到端，用 Fake 等於群面那條路
整條沒被真實作驗證過。

### 後兩項回空是刻意的，不是還沒寫完

缺的不是程式碼是**資料**——前端只把使用者自己的發言寫進 session。

我特別**沒有做近似**。想得到的替代品（數「剛剛那位」「我同意」這類詞）
測的是「有沒有做出回應的姿態」，不是「有沒有接住論點」——那會產出一個
看起來合理、實際上量錯東西的數字，**比沒有數字糟**，因為它看起來像做完了。

前端補完之後那兩項還要等 rubric（「傾聽與回應」定義成什麼會決定我算什麼訊號），
所以它有兩個外部依賴。

### 兩個實作決定

**發言次數加了 `bg_` 前綴。** 合約允許把它放進 `signals` 當背景資訊，
但「放進去再叫 LLM 不要依賴」是很弱的保護——它是那堆數字裡最直觀的一個。
加前綴讓那道界線在**資料裡**看得見，你的 prompt 可以據此把 `bg_` 開頭的
放進「背景」而不是「依據」。**這是我單方面加的慣例，你要拆成獨立欄位
（例如 `context`）我跟著改**——重點是那道界線不能只存在於註解裡。

**`first_speak_position` 的分母是「別人給了幾次機會」，不是總則數。**
我一開始寫成總則數，測試抓到了：同樣在第 2 則開口，若使用者後面多講 5 次，
位置就從 1.00 變成 0.17，看起來「開口早很多」——發言量從後門混進了那個訊號，
而 babble 假說正是要避免這件事。

## 2. `TranscriptAnalyzer` 的 singleton 併發修正 ★ 你 W3 接線一定要知道

合約要求它是 app 啟動時的 singleton（bge-m3 約 2.3GB），你 W3 就要照著接
FastAPI lifespan。但 `near_misses` 和 `residuals` 原本寫在**實例**上：

**併發污染** —— 實測兩支執行緒交錯呼叫，A 的請求讀到 B 的結果。
你若拿 `near_miss` 寫 why（「你其實碰到邊了」），會產出一段**針對別人
逐字稿的具體回饋**——錯得很有說服力。

**跨請求累積** —— `residuals` 只 append 不清空，長時間執行會持續長大。

兩個在單執行緒測試裡永遠是綠的。

### API 變更

```python
analyzer.near_misses()                # 舊：已移除，呼叫會 AttributeError
analyzer.residuals()                  # 舊：同上

result = analyzer.analyse(t)          # 新：-> MentionResult
result.mentions / result.near_misses / result.residuals
```

`mentioned_skills()` 與 `mentions()` 沒變，只用那兩個就不受影響。

舊方法改成**明確報錯而不是回空值**——回空值會讓呼叫端以為「這次沒有
near_miss」，而不是「這個 API 不能用了」。

---

# 十、抽取器實測：正式環境的漏講功能還不能用 ★

你給的 `jd_variants_extracted.json` 接進 `--jd-source extracted` 量完了。

## 數字

```
               catalog    extracted(表面)   extracted(語意段)
JD 集 recall    1.000        0.138             0.200
漏講 recall     1.000        0.286             0.375
真漏講          8 筆         2 筆              3 筆
```

同一組人工標記、同一份逐字稿，只換 JD 來源。`wants` 是人對 JD 的判斷，
跟 `required_skills` 怎麼產生的無關，所以這個差額就是
**抽取損失的直接測量**（零額外標記成本，這是 D1 提的雙變體設計的用途）。

**語意段方向對但量級不夠**——0.138 → 0.200，離可用還很遠，
`漏講 recall 0.375` 沒過 0.700 的門檻。所以問題在抽取器本身，
不是後段補得回來的。

## 一個要小心的數字

**`漏講 precision` 在 extracted 是 0.750，比 catalog 的 0.727 還「好看」。**

那是分母只有 4 筆造成的假象——只報 4 筆當然錯得少，代價是漏掉 5 筆真的。

這跟 W1 那次「詞彙表覆蓋率 32% 讓提及 recall 上升」是同一個形狀：
**難的案例被移出考卷，分數自然好看。** 這種偏誤沒有任何跡象，
報表照樣印出漂亮的小數點。

閘門這次擋對了（`漏講 recall 0.375 < 0.700` 直接判不通過），
沒被 precision 的假象蓋過去——當初把兩個方向分開報是對的。

## 問題不只是抓得少，是產出的詞跟詞彙表不同源

```
ivw-005  型錄 13 條 → 抽取 5 條：資料蒐集、市場觀察、計畫制定、策略制定、提案設計
ivw-008  型錄 11 條 → 抽取 10 條：應付帳款、原始憑證、傳票、網銀匯款、營業稅
```

語意上都沒錯，但 `計畫制定` 是**文案改寫**不是技能表的詞，
`應付帳款`／`傳票` 是**業務名詞**不是技能。
字面重疊率 11%（13/118），語意段也只補回一小部分。

**建議**：抽取 prompt 改成**從詞彙表選詞，不要自由生成**——
給它 222 條 `name_zh` 當候選清單，讓它挑而不是寫。

這跟你在 `REQUEST_connector_rate.md` 裡歸納的是同一件事：
「三次都是把有唯一正確答案的事交給了 LLM，改成公式之後三次都解決。」
這是第四次。

---

# 十一、那 224 筆未標記：實際要標的只有 3 筆

`tools/triage_unlabeled.py` 分成四類，只有前三類該標：

| 類 | 條件 | 影響 | 數量 |
|---|---|---|---|
| A | 履歷∩JD 都有 | **會產生 gap** | 0 |
| B | 只在 JD 側 | JD 集 precision | **3** |
| C | 只在履歷側 | 履歷集 precision | 0 |
| D | 只在提及集 | 進不了交集，永遠變不成漏講 | 221 |

**要標的 3 筆**：`Line`、`行政事務處理`、`零用金管理`。
清單在 `data/unlabeled_for_review.json`。

`Line` 那筆別混：它在 `ivw-005` 是我標的**假陽性測資**
（「設一個在LINE」其實是「設一個 deadline」被 STT 切壞），
但它在 JD 側是**真的出現**。兩件事。

## D 類佔 98.7% 本身是個發現

語意段的誤命中量比我原本以為的大得多，**只是那些誤命中剛好落在候選池外**，
進不了 `履歷 ∩ JD` 所以變不成漏講。

換一批 JD 或不同履歷就未必。這要寫進限制——
第十三節的「提及 precision 是用來換 recall 的」那條，
實際幅度比報表上的 0.729 顯示的更大。

---

# 十二、`connector_rate`：做了，但只做因果類

回應 `REQUEST_connector_rate.md`。你的三個問題我拿六段逐字稿都驗了。

## 問題 1（「然後」）：你的擔心成立，但排除序列類就解決

「然後」39 次，比因果 16 次還多一倍，而且它已在 `FILLERS` 裡。

但**因果類不含「然後」**，所以打架只發生在序列類——
不必做上下文判斷（你的 b 方案），排除序列類即可，結論接近你傾向的 c。

## 問題 2（STT 轉錯）：不是轉錯，是根本沒出現

**轉折類在六段 2,477 字裡出現 0 次。** 不是稀少，是完全沒有。
「有沒有處理對立面」這個維度在真實逐字稿上量不到。

序列類扣掉「然後」只剩 10 次，散在「最後」「後來」「之後」「第一」四個詞。

所以三類裡有兩類不成立。**做一個永遠是 0 的欄位比沒有欄位更容易被誤用**，
那兩類我不做。

## 問題 3（錨點）：有，但比填充詞弱很多

```
每百字因果詞    錄音指示
0.26          Helen 正常
0.00          Helen 講滿
0.00          Victor 正常
1.01          Victor 只講故事
1.50          Ken 正常
0.72          Ken 刻意講亂
```

問題在 **T3 與 T5：同樣是「正常發揮」，一個 0.00 一個 1.50，橫跨整個量程。**
那個差異來自個人講話習慣，不是邏輯清晰度。

填充詞那組是 3.0／6.4／9.8 單調對應錄音指示，這組不是。

好消息：因果密度跟填充詞密度**沒有反向打架**（r = 0.224，n=6 只能看方向）。

## 交了什麼

`connector_rate`（每百字因果詞）＋ `connector_detail`（只有 `causal`），
**並在欄位註解寫明錨點不可靠、建議當背景資訊而非公式輸入**。

這正是你文件最後說的：「不要為了補這個欄位而做一個測不準的東西。」
邏輯清晰度可能還是得維持印象分，資訊夠你判斷了。

**★ 那兩個欄位我先加在本機的 `interview_protocols.py` 跑，正式版要你加：**

```python
connector_rate: float = 0.0
connector_detail: dict[str, int] = field(default_factory=dict)
```

---

# 十三、限制（要寫進報告）

**單一講者。** 六段逐字稿都是怡君錄的。

**有效樣本數是 6 不是 10。** 同段逐字稿服務多格，誤差相關。
所有「recall 1.000」這類保證都建立在這 6 段上。

**提及 precision 是用來換 recall 的。** 0.952 → 0.729 是刻意接受的取捨，
方向安全但幅度不小。樣本一大要重新檢視。

**bge-m3 對「行為 vs 標籤」的語意距離是這個方法的天花板。**
不是參數沒調好，是方法本身到這裡。
