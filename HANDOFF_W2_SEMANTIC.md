# W2 A 側交接：語意段校準（A → B）

分支：`CareerSandboxInterview` → `feat/a-semantic`

W2 D3–D5 的「A5 門檻調參」做完了。三個參數從「憑常識填」變成有實測依據，
**假指控從 9 筆降到 3 筆**，四個門檻全數通過。

---

# 一、先跑這個看數字

```bat
git fetch && git checkout feat/a-semantic
python tools\interview_eval.py --cases fixtures/golden/interview ^
    --vocab fixtures/vocab/skills_v1.json ^
    --stt-aliases data/stt_confusions.v1.json ^
    --semantic --by-case
```

**會載 bge-m3（約 2.3GB），跑一分鐘左右。**

只想秒跑的話拿掉 `--semantic`——但那量的是表面掃描，不是完整管線，
數字會是舊的 0.471。報表最上方會標明模式，不會搞混。

---

# 二、W2 的結果

| | W1 結束 | W2 結束 |
|---|---|---|
| 履歷集 | 1.000 / 1.000 | 1.000 / 1.000 |
| JD 集 | 0.965 / 1.000 | 0.965 / 1.000 |
| 提及集 | 0.952 / **0.488** | 0.729 / **0.854** |
| 漏講 | **0.471** / 1.000 | **0.727** / 1.000 |
| 排序（人工首選落在前三名） | 0.600 | **1.000** |

**假指控 9 → 3，而真漏講一筆都沒漏**（漏講 recall 全程 1.000）。

## 校準後的三個參數

```
SEMANTIC_WINDOW      28 → 16      語意段的滑動視窗
MENTION_THRESHOLD  0.55 → 0.45    一般門檻
CANDIDATE_THRESHOLD     → 0.40    履歷候選的放寬門檻（新增）
```

**視窗比門檻更關鍵**，這點我原本沒想到。實測 recall：視窗 12 → 0.805、
16 → 0.805、20 → 0.707、28 → 0.561。原因是長視窗把一整段話拿去跟四個字的
技能名比相似度，中間的「我那時候」「然後」全是雜訊，餘弦分數被稀釋。

門檻取 0.45 而非掃描器推薦的 0.40：視窗 16 在 0.40／0.45／0.50 三個門檻的
四項指標完全相同，是一片**平原**；0.40 在掃描範圍邊緣，不知道再低會不會崩。
**參數落在平原中間比落在邊緣穩健。**

---

# 三、對你 prompt 最重要的一件事

## 剩下 3 筆假指控的成因**全部是展演式陳述**

```
ivw-001／002   軟體程式設計
ivw-006        專案管理
```

**沒有一筆是 STT 轉爛。** `Git`／`JavaScript`／`Angular` 全被語意段救回來了——
這比我預期的好，我原本以為「gats」跟 Git 在語意空間沒有關係。

剩下這兩個是「做了但沒說出名字」。Victor 講的是
「我負責約訪談時間、有人拖延就傳訊息設 deadline、追他在期限前完成」，
但那跟「專案管理」這四個字的語意相似度**降到 0.20 都構不到**——
不是「差一點」，是根本不在附近。

原因：**抽象標籤跟具體行為在 bge-m3 的空間裡屬於不同的語意層級。**
這不是門檻問題，也不是 bug，是目前方法的限制。

### 所以你的 why prompt 要注意

漏講清單裡若出現**抽象能力型**的技能（專案管理、軟體程式設計、溝通協調
這一類），它是假指控的機率明顯高於工具型技能（Excel、Git、Python）。

工具型的漏講現在幾乎都是真的——語意段＋STT 別名把偽陽性清掉了。
抽象能力型的還有殘留。

不是要你分兩套語氣（我 W1 提過、你當時說過濾優於委婉語氣，那個判斷我同意），
而是**如果你的 why 否決機制要設優先序，抽象能力型該排在前面被檢視**。

---

# 四、兩個代價，講清楚

## 提及集 precision 0.952 → 0.729

誤抓從 1 筆變 13 筆：`HTML/CSS`、`會計或審計`、`簡報表達`、`文中系統` 等
被算成講到了。

方向是安全的（少給一條建議 vs 假指控），而且**漏講 recall 維持 1.000**
證明沒有吃掉真漏講。但這個保證建立在 6 段逐字稿上，樣本一大未必成立。

## 未標記從 32 筆暴增到 224 筆 ← 值得你看一眼

語意段會對整份 222 條詞彙表做最近鄰，掃出一堆候選池外的東西：
`Gemini`、`A/B 測試`、`主動聆聽`、`Claude`⋯⋯

它們不計分所以不影響指標，但**它說明語意段在放寬的同時也在大量誤命中**，
只是那些誤命中剛好落在候選池外。換一批 JD 或一份不同的履歷就未必。

這也表示 JD 集那個 0.965 仍然是虛的——那 224 筆沒人標過。
（W1 交接時是 29 筆，你還沒標，現在變 224 筆了。標記量變大了，抱歉。）

---

# 五、沿路抓到兩個「沒報錯」的漏

兩個都是同一類：**東西是對的，但接的方式錯了，而測試全綠。**

## 1. `stt_confusions.v1.json` 做好兩週，沒有任何程式讀過它

W1 探測完就產出的 STT 轉寫對照表，躺在 `data/` 裡看起來像交付物，
實際上是一份文件。`TranscriptAnalyzer` 有 `stt_aliases` 參數，只是沒人傳。

接上之後提及 recall **0.829 → 0.854**（剛好越過門檻），
`C#` 從漏抓清單消失——那是 `C Sharp`，C# 的口語形，不是辨識錯誤。

`interview_eval` 現在有 `--stt-aliases` 旗標。**碰撞別名（`Android`→`Angular`）
不載**，那要靠履歷條件判定，載進去會讓真正的 Android 永遠抓不到。

## 2. 接上它的時候打亂了索引優先序

`SurfaceIndex.from_vocab` 本來是兩趟：先登記**全部**完整表面形，再登記詞頭，
`setdefault` 保證完整形優先。我把別名合併寫在外面、逐條目做「完整形→詞頭」，
於是**第一條的詞頭贏過第二條的完整形**。

「報表彙整與管理」的詞頭是「報表彙整」，剛好撞上「報表彙整」這個獨立條目——
同一句話解析到不同技能，不當掉、不報錯，只有履歷集 recall 從 1.000 掉到 0.958。

如果我沒盯著那一欄，這會被當成「載入別名的正常代價」放過去。

已改成 `from_vocab` 直接吃 `extra_aliases`，三趟全域分開：
**完整形 → STT 別名 → 詞頭**。加了測試守著。

---

# 六、工具與 baseline 的變更

## `interview_eval` 新增三個東西

| 旗標 | 用途 |
|---|---|
| `--semantic` | 開語意段。預設關閉（你覆核黃金集時秒跑完） |
| `--stt-aliases` | 載 STT 轉寫對照表 |
| `--by-case` | 逐格分解。**「哪一格壞掉」比「整體平均」有用得多** |

## baseline 分兩份，而且不能互比

```
data/interview_baseline_surface.json    表面掃描（漏講 P 0.471）
data/interview_baseline_semantic.json   語意段（漏講 P 0.727）
```

檔名自動帶模式，檔案內部也記模式與三個參數。**拿 surface baseline 去跟
semantic 模式比對會被擋下**——兩者量的是不同管線，混在一起會讓回歸失去意義：
看到「退步」時分不出是程式壞了還是模式不同。

參數跟數字一起凍也是為此——回歸時看到差異，要能分辨是程式改了還是參數改了。

## 四個防呆

模式不符的 baseline 會擋下；模型載不起來**直接炸而不退回表面掃描**；
假向量拒絕寫 baseline；未覆核的草稿標記拒絕寫 baseline。

第二個最重要：退回之後數字看起來完全合理（0.471 是個正常數字），
但它會被貼上 `semantic` 的標籤凍進 baseline，之後每次回歸都在跟一個
標錯模式的基準線比，而且沒有任何跡象。

---

# 七、營運提醒

**語意段要載 bge-m3（約 2.3GB）。** 合約 docstring 已註明
`TranscriptAnalyzer` 必須是 app 啟動時載入的 singleton，
不能每個 request new 一個。你端點組裝時的 FastAPI lifespan 要涵蓋它。

**詞彙表向量有全域快取**（`transcript._VOCAB_VEC_CACHE`）。
沒有它的話調參會跑到天亮——實測 4 視窗 × 7 門檻 × 10 格 = 14.6 萬次向量化、
2.7 小時。加了快取加上 `_semantic_pass` 不再內建門檻，整輪掃描只算一次向量。

正式環境詞彙表不變，所以那份快取一次算完就固定。**但詞彙表若在 runtime
被換掉，快取的 key 是 `id(embedding)` 加表面形 tuple，會自動失效，不用手動清。**

---

# 八、還沒做的

## `CollabScorer`（W2 D1–D3 的格子）

被兩件事卡著：

**rubric 還在委外那邊。** 那份文件的管線是「A 給線索 → LLM 對照 BARS 給等第」，
跟合約的 `CollabRaw.score: int 0–100`（我直接給分數）是兩種架構。
我傾向文件那版——BARS 錨點是行為描述（「僵局時推進」），判斷某段發言算不算，
本質上要語意判斷。**這個要你定案，合約可能要再改一次。**

**前端的發言者記錄。** 怡君確認會補，趕得上 W3。在那之前
「傾聽與回應」和「協作姿態」不管 rubric 怎麼寫都算不出來——
資訊不在場。

## 那 3 筆假指控

要解決得把技能的**行為描述**放進詞彙表當別名
（「排時程、追進度、設期限」→ 專案管理），拿行為對行為比。

那是詞彙表工程不是參數調整，工作量不小。**W3 值得評估，但不該倉促做**——
現在 0.727 已經是可用的數字。

## 那 224 筆未標記

見第四節。JD 集的 0.965 因此仍是虛的。

---

# 九、限制（要寫進報告）

**單一講者。** 六段逐字稿都是怡君錄的。

**有效樣本數是 6 不是 10。** 同段逐字稿服務多格，誤差相關。
所有「recall 1.000」這類保證都建立在這 6 段上。

**提及 precision 是用來換 recall 的。** 0.952 → 0.729 是刻意接受的取捨，
方向安全但幅度不小。樣本一大要重新檢視。

**bge-m3 對「行為 vs 標籤」的語意距離是這個方法的天花板。**
不是參數沒調好，是方法本身到這裡。
