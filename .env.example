# 複製成 .env 後填入實際值。.env 不進版控。

# LLM
OPENAI_API_KEY=
# 模型名稱必須與既有模型服務一致,不可自行更動
OPENAI_MODEL=gpt-4o-mini
# STAR 拆解需要跨行 verbatim 引用,mini 做不到,單獨指定
OPENAI_MODEL_VERBATIM=gpt-4o

# 向量查詢(A5 若沿用既有檢索時需要)
EMBEDDING_MODEL=bge-m3
